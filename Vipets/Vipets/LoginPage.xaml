<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Vipets"
             x:Class="Vipets.LoginPage">
    <StackLayout>
        <Label Text="Login Page" FontSize="Large"
               HorizontalOptions="CenterAndExpand" VerticalOptions="StartAndExpand" />

        <Entry x:Name="email" Placeholder="email" FontSize="Small" 
               HorizontalOptions="Fill" VerticalOptions="Center"  />

        <Entry x:Name="password" Placeholder="password" IsPassword="true" FontSize="Small"
               HorizontalOptions="Fill" VerticalOptions="Center" />

        <Button Text="Enter" Clicked="Login_Clicked"
                HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" />

    </StackLayout>
</ContentPage>