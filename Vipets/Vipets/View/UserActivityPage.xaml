<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Vipets.View"
             x:Class="Vipets.View.UserActivityPage">
  <ContentView.Content>
      <StackLayout>

            <ListView x:Name="Activty_List">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding petName}" Detail="{Binding activityDesc}">
                            

                        </TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackLayout>
  </ContentView.Content>
</ContentView>